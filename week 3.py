print ("How many obstacles must I avoid?")
obstacles_to_avoid = int(input())

obstacles = 0
while obstacles_to_avoid<obstacles_to_avoid:
    print ("Avoiding...")
    obstacles_avoided += 1
    print(f"Done! {obstacles_avoid} obstacles avoided.")